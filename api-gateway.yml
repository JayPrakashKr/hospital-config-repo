server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:

        - id: patient-service
          uri: lb://patient-service
          predicates:
            - Path=/patients/**

        - id: doctor-service
          uri: lb://doctor-service
          predicates:
            - Path=/doctors/**
